import { HasAuthorityDirective } from '../components/has-authority.directive';

describe('HasAuthorityDirective', () => {
  it('should create an instance', () => {
    const directive = new HasAuthorityDirective();
    expect(directive).toBeTruthy();
  });
});
